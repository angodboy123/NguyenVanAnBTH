@{
    ViewData["Title"] = "Home Page";
}
<h2>Home</h2>
<form asp-action="Index">
    <label for="Nhập tên ">Nhập tên</label>
    <input type="text" name="FullName">
    <input class="btn btn-danger" type="submit" name="Save">
</form>
<h2> @ViewBag.name</h2>